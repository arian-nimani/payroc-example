const VITE_BASE_API_URL = import.meta.env.VITE_BASE_API_URL;
const VITE_SESSION_TOKEN_PATH = import.meta.env.VITE_SESSION_TOKEN_PATH;

const payrocConfig = {
  VITE_BASE_API_URL,
  VITE_SESSION_TOKEN_PATH,
};

export default payrocConfig;
